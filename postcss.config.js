/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 integration note:
 * - Theme configuration is embedded in src/index.css via @theme inline
 * - No separate tailwind.config.js required (v4 feature)
 * - @tailwindcss/vite plugin handles processing via Vite
 * - This config kept minimal for future extensibility
 */

export default {
  plugins: {},
}
